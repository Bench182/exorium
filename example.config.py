import mysql.connector

token = 'AUTHTOKEN'

color = (COLOR)

key = 'pixabay api key'


DBdata = mysql.connector.connect(
  host="localhost",
  user="username",
  password="password",
  database="database name"
)
